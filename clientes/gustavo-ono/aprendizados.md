# Aprendizados — Gustavo Ono

Log do que funciona e do que não funciona com este cliente. Toda sessão pode
adicionar uma linha. É a memória que se acumula ao longo do tempo.

| Data | Aprendizado / decisão | Origem (campanha, teste, call) |
| ---------- | --------------------- | ------------------------------ |
| 2026-04-30 | Abril foi o primeiro mês com vendas de mentoria HT (2 vendas = R$10k) + 5 low tickets. CPL R$23,8, ROAS 7,73. | Controle de Métricas |
| 2026-04-30 | Em Fev e Mar o funil gerou leads a CPL baixo mas zero vendas HT. Problema estava no comercial, não no tráfego. | Controle de Métricas |
| 2026-05-31 | Maio teve melhor CPL (R$15,3) e mais leads (84), mas zero vendas. Indica gargalo no comercial/agendamento. | Controle de Métricas |
| 2026-06-03 | CPL subiu para R$52 em Junho (acima do benchmark R$32). Precisam otimizar criativos ou segmentação. | Growth Model 2026 |
| 2026-06-03 | Taxa MQL de 43% em Junho — qualidade dos leads é boa; problema é taxa de agendamento/comparecimento. | Pit Stop Estratégico |
| 2026-06-08 | Cadências de reativação (frios e mornos) criadas — 5 toques em 10 dias para frios, 4 toques em 8 dias para mornos. | Copy |
| 2026-07-02 | ICP confirmado: mulher 35–55 anos, maioria já tem chocolateria, frustrações com inconsistência e precificação. | Planilha de Leads / Quiz 2 |
| 2026-07-02 | Público de maior ticket médio declarado (acima de R$30k faturamento) aparece nos leads do Quiz 2 — vale segmentar. | Planilha de Leads |
| 2026-07-15 | Meta Pixel (ID 413208557089573) instalado no quiz: PageView na landing e no relatório, ViewContent ao abrir relatório, InitiateCheckout nos CTAs, Lead no form, CompleteRegistration ao iniciar quiz. Usar `fbqTrack()` de `src/analytics.ts` — nunca chamar `window.fbq()` diretamente. | Deploy quiz-gustavo-ono |
| 2026-07-15 | O projeto Vercel `quiz-gustavo-ono` **não tem integração GitHub** — push no git não dispara deploy automático. Para subir: rodar `VERCEL_TOKEN=xxx npx vercel deploy --prod --yes` dentro de `funis/quiz-gustavo-ono/`. | Deploy quiz-gustavo-ono |
| 2026-07-15 | O `vercel.json` precisa declarar `buildCommand`, `outputDirectory` e `framework` explicitamente — sem isso a Vercel serve os arquivos-fonte sem compilar (página preta no browser). | Deploy quiz-gustavo-ono |
| 2026-07-15 | noscript do Meta Pixel deve ficar no `<body>`, não no `<head>` — Vite/parse5 rejeita conteúdo não-script dentro do noscript em head e quebra o build. | Deploy quiz-gustavo-ono |
| 2026-07-22 | **Ad blockers bloqueiam silenciosamente fetch() do browser para `hook.us2.make.com`** — Make.com registrou zero execuções de 14 a 21/julho, leads sumiam sem rastro. Fix: criar função serverless Vercel em `api/lead.js` que recebe o lead no servidor e faz proxy para o Make.com. O `WEBHOOK_URL` no `App.tsx` deve apontar para `/api/lead` (same-origin), nunca para o endpoint externo direto. O `vercel.json` precisa de rewrite com negative lookahead `/((?!api/).*)`  para não capturar `/api/` no catch-all do SPA. | Diagnóstico quiz-gustavo-ono |
| 2026-07-22 | Para deploy no Vercel: `VERCEL_TOKEN=xxx vercel deploy --token "$VERCEL_TOKEN" --scope simpleacc --prod --yes` (token como argumento separado, não `--token=valor` — o CLI rejeita o formato com `=`). | Deploy quiz-gustavo-ono |
| 2026-07-23 | Fontes auto-hospedadas via `@fontsource-variable` (Inter + Lora) eliminam request externo bloqueante do Google Fonts — maior ganho de velocidade mobile. Instalar com `npm i @fontsource-variable/inter @fontsource-variable/lora` e importar em `src/index.css`. | Otimização mobile quiz-gustavo-ono |
| 2026-07-23 | Inline placeholder `<div style="min-height:100dvh;background:#1a0900">` dentro do `#root` no `index.html` elimina o flash branco enquanto o JS carrega — usuário já vê o fundo escuro correto na primeira pintura. | Otimização mobile quiz-gustavo-ono |
| 2026-08-18 | A planilha de leads é **compartilhada entre os quizzes de todos os clientes**, cada campanha numa aba. A aba `quiz novo` não era só do Gustavo. Campanha nova = aba nova. A desta é **`diagnostico gustavo`**. | Correção de rota da planilha |
| 2026-08-18 | Dois bugs de medição que existiam desde o começo: o Pixel recebia **`ViewContent` a cada pergunta respondida** (cerca de nove por lead, colidindo com o ViewContent do relatório) e **`InitiateCheckout` ao chegar no formulário**, não só no clique do treinamento. Ambos corrompiam otimização de campanha. Agora avanço de pergunta e chegada no formulário são só dataLayer (`quiz_pergunta`, `quiz_formulario`). | Revisão de tracking |
| 2026-08-18 | Eventos no dataLayer para criar acionadores no GTM: `quiz_iniciado`, `quiz_pergunta`, `quiz_formulario`, `quiz_lead`, `relatorio_visto`, `clique_whatsapp`, `clique_treinamento`. Antes o container carregava e não recebia nada do funil. | Revisão de tracking |
| 2026-08-18 | O `read_file_content` do Drive **não expõe nome nem gid das abas** e trunca planilhas grandes. Para criar aba ou descobrir estrutura, montar cenário temporário no Make (`google-sheets:addSheet` usa `mapper.properties.title`, não `title`). A saída dos módulos não é legível pelo MCP, então o resultado precisa ser gravado em algum lugar e lido depois. | Automação Make |
| 2026-08-12 | Container GTM real (`GTM-WGMRWRSS`) instalado no `index.html`. Desde o primeiro deploy o quiz carregava `GTM-PLACEHOLDER`, ou seja, nenhum evento chegava ao GTM. O Clarity **continua com `CLARITY_PLACEHOLDER`** e faz uma requisição 404 a cada pageview. | Instalação de tracking |
| 2026-08-12 | Integração do Make atualizada e testada: colunas `Rota` (R) e `Curso Anterior` (S) criadas na aba `quiz novo`, mapeamento do cenário 5619844 ajustado, `dominasse` aposentado. Blueprint anterior salvo em `funis/quiz-gustavo-ono/make/`. Para criar colunas numa planilha sem ferramenta de escrita em Sheets, dá para montar um cenário temporário no Make com `google-sheets:updateCell` (exige `valueInputOption`), rodar uma vez e apagar. | Implementação da proposta |
| 2026-08-12 | Preview da Vercel não abre sem login: o projeto tem SSO Protection em `all_except_custom_domains`. Para mandar link testável a quem não é do time, ou se desliga a proteção de preview, ou se promove para produção, ou se usa o domínio customizado. | Deploy de preview |
| 2026-08-07 | **Produção estava à frente do Git.** A faixa de faturamento "Acima de R$ 20.000" estava no ar mas o repositório ainda tinha "Acima de R$ 30.000". Como o projeto Vercel não tem integração com GitHub e o deploy é manual via CLI, alterações feitas direto podem não voltar para o Git. Antes de mexer no quiz, conferir o bundle em produção (`curl` no `index.html`, achar `/assets/index-*.js`, comparar strings). | Implementação da proposta aprovada |
| 2026-08-07 | Roteamento de leads no fim do quiz aprovado pelo cliente e implementado. Rota A (carro-chefe ou <50% das vendas **e** faturamento acima de R$ 5k) vê a sessão diagnóstica; Rota B vê o Treinamento de R$ 97 como oferta principal, com a sessão inclusa como bônus da compra (Rota C). A lógica fica isolada em `src/lib/rota.ts` — mexer só ali. | Proposta 2026-08-07 |
| 2026-08-07 | `VITE_WHATSAPP_NUMBER` passou a ser variável de ambiente (antes era constante no código). **A Rota A inteira depende dela**: sem o número, o CTA de agendamento não abre conversa. Precisa estar nas Environment Variables da Vercel, não só no `.env` local. | Implementação da proposta |
| 2026-08-07 | O payload do webhook ganhou o campo `rota` (A ou B) e o campo `dominasse` virou `curso_anterior`. **O cenário do Make e a planilha de leads precisam das duas colunas novas**, senão os dados são descartados silenciosamente. | Implementação da proposta |
| 2026-08-18 | Número de WhatsApp do Gustavo configurado em `VITE_WHATSAPP_NUMBER` nas Environment Variables da Vercel (production, preview e development). O número **não fica no Git** — só o `.env.example` com um valor de exemplo. Sem essa variável no ambiente de build, o CTA da Rota A sai apontando para `wa.me/` vazio. | Deploy quiz-gustavo-ono |
| 2026-08-18 | Snippet solto do Clarity removido do `index.html`. Rodava com ID de exemplo desde o primeiro deploy e só gerava um 404 por pageview. Se um dia quisermos gravação de sessão, publicar a tag pelo GTM (`GTM-KW7KM3LM`). | Instalação de tracking |
| 2026-08-18 | O repositório local no container da web pode acordar **atrás do remoto** (aconteceu duas vezes: HEAD em `303d8fd` com origin em `739b7d6`). Antes de mexer em qualquer coisa, rodar `git fetch origin <branch> && git reset --hard origin/<branch>` e depois `npm install` dentro do funil — o `node_modules` também fica defasado e o build quebra em `@fontsource-variable/cormorant-garamond`. | Sessão de deploy |
| 2026-07-23 | ReportScreen lazy-loaded com `React.lazy()` + `Suspense` — chunk separado de 14 kB que só carrega quando o usuário chega em `/resultado`, reduzindo o bundle inicial. Manual chunks no Vite (`vendor-react`, `vendor-router`) separam deps cacheáveis do código da aplicação. | Otimização mobile quiz-gustavo-ono |
| 2026-08-18 | Os eventos `quiz_lead`, `relatorio_visto`, `clique_whatsapp` e `clique_treinamento` passaram a levar o parâmetro `rota`. O relatório é uma página diferente em cada rota, então medir clique no low ticket sem separar por rota mistura duas ofertas distintas. Roteiro completo de configuração do GTM em `funis/quiz-gustavo-ono/TRACKING.md`. | Revisão de tracking |
| 2026-08-18 | A tag `[Meta Ads] 1 | PageView` do container `GTM-KW7KM3LM` duplica o pixel: o `index.html` já inicializa o Pixel e o app dispara `PageView` na landing e no relatório. Pausar a tag no GTM, não tirar o pixel do código — os eventos de Lead e InitiateCheckout saem do React e dependem do `fbq` já existir na montagem. | Revisão de tracking |
| 2026-08-18 | **O Meta Pixel saiu do `index.html` e passou a ser carregado por tag no GTM.** O código não chama mais `fbq`: `src/analytics.ts` só empurra para o dataLayer e o container distribui para GA4 e Pixel. Ganho: evento ou ferramenta nova não pede deploy. Custo: sem container publicado, nada é medido — não existe mais pixel de reserva na página. Passo a passo em `funis/quiz-gustavo-ono/TRACKING.md`. | Migração de tracking |
| 2026-08-18 | Ordem de subida quando se mexe em pixel: **publicar o GTM primeiro, subir o deploy depois**. Ao contrário, existe uma janela em que o pixel já saiu do código e a tag ainda não está no ar, e a Meta não recebe nada. | Migração de tracking |
| 2026-08-18 | Dá para conferir se um container do GTM está publicado sem abrir o painel: `curl "https://www.googletagmanager.com/gtm.js?id=GTM-XXXX"` e procurar as marcas das tags (`fbq`, `G-`). Alterações que estão só no workspace não aparecem ali. | Migração de tracking |
| 2026-08-19 | Número do WhatsApp do diagnóstico trocado (o anterior durou um dia). Trocar é só `vercel env rm` + `vercel env add` nos três ambientes e um deploy novo: como é variável de build, **não basta mudar na Vercel, precisa reconstruir**. Nenhum número passa pelo Git. | Deploy quiz-gustavo-ono |
| 2026-08-19 | Container `GTM-KW7KM3LM` publicado com o Meta Pixel e as tags de `Lead`, `InitiateCheckout`, `ViewContent` e `Contact` nos acionadores do funil. Com isso o deploy que tirou o pixel do `index.html` pôde subir sem janela cega. Falta a tag de `CompleteRegistration`: o acionador `quiz_iniciado` não existe no container publicado. | Migração de tracking |
| 2026-08-19 | **Buraco de captação encontrado e corrigido no cenário 5619844.** O módulo de criar contato no GHL roda com `onerror: Ignore`, que encerra o pacote: uma lead recusada pelo GHL sumia inteira, sem linha na planilha, e a execução aparecia como sucesso com 2 operações em vez de 4. Cerca de 10 das 45 execuções históricas estavam nesse estado. Ordem invertida para **webhook → planilha → GHL → oportunidade**: agora falha de CRM custa só o registro no CRM. | Preflight da campanha |
| 2026-08-19 | Ao auditar um cenário do Make, **contagem de operações é o sinal mais barato**: execução com menos operações que o fluxo completo, mas marcada como sucesso, quer dizer que algum `onerror: Ignore` engoliu uma falha no meio do caminho. O `executions_get-detail` do MCP não devolve o detalhe por módulo, então o jeito de investigar é disparar o webhook em produção e comparar a contagem. | Preflight da campanha |
