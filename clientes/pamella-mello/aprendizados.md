# Aprendizados — Pâmella Mello

Log do que funciona e do que não funciona com este cliente. Toda sessão pode
adicionar uma linha. É a memória que se acumula ao longo do tempo.

| Data | Aprendizado / decisão | Origem (campanha, teste, call) |
| ---------- | --------------------- | ------------------------------ |
| 2026-07-15 | O material desta cliente (dossiê, estratégia, canvas, planilha de leads) já existia no Google Drive e no ClickUp desde antes deste repo ter uma pasta pra ela — nada estava versionado no Git. Estruturamos `clientes/pamella-mello/` agora; a partir daqui, novas alterações devem entrar pelo Git, não só pelo Drive. | Resgate de sessão anterior via Drive/ClickUp |
| 2026-07-15 | O projeto `funil-pamella-mello` na Vercel **não está conectado a um repositório Git** — foi implantado direto via CLI/API por uma sessão anterior. Isso quebra a premissa "Memória = Git" do hub: o código só existia no build da Vercel. Copiamos o código-fonte para `funis/funil-pamella-mello/` neste commit; deploys futuros continuam exigindo reimplantação manual até conectar o projeto ao Git (Root Directory apontando pra essa pasta). | Configuração de domínio / instalação de tags |
| 2026-07-15 | Ao adicionar um domínio customizado novo na Vercel (`quiz.pamellamellohipnoterapia.com.br`), a Vercel `POST /v10/projects/{id}/domains` retorna `verified:true` na hora, mas o edge/CDN pode continuar servindo, por alguns minutos, uma versão desatualizada (cacheada) do conteúdo, mesmo com `Cache-Control: max-age=0`. Query string de cache-busting não resolve; é preciso reimplantar/aguardar propagação e reverificar antes de considerar a mudança concluída. | Configuração de domínio (quiz.pamellamellohipnoterapia.com.br) |
| 2026-07-15 | Ao reimplantar um projeto Vercel sem framework/build (deploy estático via API), o caminho dos arquivos no payload de deploy precisa espelhar exatamente a URL final (ex.: `index.html` na raiz) — usar um prefixo como `src/` (que aparecia na listagem de arquivos do deployment antigo) quebra o roteamento e derruba o site com 404. | Reinstalação do GTM (deploy via API Vercel) |
| 2026-07-15 | Cliente pediu instalação de pixel Meta (`328694529132563`): já estava instalado (por sessão anterior) no quiz e no diagnóstico, incluindo eventos customizados (`InitiateCheckout`, `Lead`, `Contact`). Confirmar sempre o estado atual antes de reinstalar. Em seguida instalamos GTM (`GTM-PWQS84LW`) nas mesmas duas páginas (head + noscript body). | Instalação de tags de tracking |
| 2026-07-15 | Boa prática a reforçar com a cliente/time: tokens de API (ex.: token da Vercel) não devem ser colados em texto de chat — ficam registrados no histórico. Quando acontece, revogar o token logo após o uso. | Configuração de domínio (token Vercel colado no chat) |
